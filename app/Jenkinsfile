pipeline {
    agent any

    environment {
        PROJECT_DIR = 'app'
        BACKEND_PORT = '8002'
        FRONTEND_PORT = '3000'
    }

    stages {
        stage('Prepare Workspace') {
            steps {
                script {
                    echo "🧼 Cleaning old workspace..."
                    cleanWs()
                }
            }
        }

        stage('Checkout Repository') {
            steps {
                script {
                    echo "📦 Checking out Git repository..."
                    checkout scm
                    echo "✅ Checkout complete."
                }
            }
        }

        stage('Verify Docker Setup') {
            steps {
                script {
                    echo "🔍 Verifying Docker environment..."
                    sh 'docker --version'
                    sh 'docker-compose --version'
                }
            }
        }

        stage('Build Docker Images') {
            steps {
                dir("${PROJECT_DIR}") {
                    script {
                        echo "⚙️ Building frontend and backend images using docker-compose..."
                        sh 'docker-compose build'
                        echo "✅ Build successful!"
                    }
                }
            }
        }

        stage('Deploy Containers') {
            steps {
                dir("${PROJECT_DIR}") {
                    script {
                        echo "🚀 Deploying containers..."
                        sh 'docker-compose down --remove-orphans || true'
                        sh 'docker-compose up -d'
                        echo "✅ Containers are up!"
                    }
                }
            }
        }

        stage('Test Services') {
            steps {
                script {
                    echo "🧪 Running health checks..."
                    retry(3) {
                        sleep(5)
                        sh "curl -f http://localhost:${FRONTEND_PORT} || exit 1"
                        sh "curl -f http://localhost:${BACKEND_PORT} || exit 1"
                    }
                    echo "✅ Both services are healthy!"
                }
            }
        }

        stage('Cleanup') {
            steps {
                script {
                    echo "🧹 Cleaning up unused Docker resources..."
                    sh 'docker container prune -f || true'
                    sh 'docker image prune -f || true'
                    echo "✅ Cleanup complete!"
                }
            }
        }
    }

    post {
        success {
            echo "🎉 Pipeline finished successfully! Both frontend and backend are running."
        }
        failure {
            echo "❌ Pipeline failed. Please check the Jenkins logs."
        }
        always {
            echo "🏁 Final workspace cleanup..."
            cleanWs()
        }
    }
}
